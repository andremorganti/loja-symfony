<form method="post" action="{{ path('app_corporate_type_delete', {'id': corporate_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ corporate_type.id) }}">
    <button class="btn btn-danger">Remover</button>
</form>
