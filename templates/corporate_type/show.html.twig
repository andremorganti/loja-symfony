{% extends 'base.html.twig' %}

{% block title %}CorporateType{% endblock %}

{% block body %}
    <h1>CorporateType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ corporate_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ corporate_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ corporate_type.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_corporate_type_index') }}" class="btn btn-secondary">Voltar</a>

    <a href="{{ path('app_corporate_type_edit', {'id': corporate_type.id}) }}" class="btn btn-warning">Alterar</a>

    {{ include('corporate_type/_delete_form.html.twig') }}
{% endblock %}
