{% extends 'base.html.twig' %}

{% block title %}Segment{% endblock %}

{% block body %}
    <h1>Segment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ segment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ segment.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ segment.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_segment_index') }}">back to list</a>

    <a href="{{ path('app_segment_edit', {'id': segment.id}) }}">edit</a>

    {{ include('segment/_delete_form.html.twig') }}
{% endblock %}
