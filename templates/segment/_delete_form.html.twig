<form method="post" action="{{ path('app_segment_delete', {'id': segment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ segment.id) }}">
    <button class="btn">Delete</button>
</form>
